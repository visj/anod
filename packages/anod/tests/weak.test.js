import { describe, it } from "node:test";
import assert from "node:assert/strict";
import { root, signal, compute, effect, batch, OPT_WEAK } from "./_helper.js";

describe("OPT_WEAK", { skip: true }, () => {
    it("placeholder", () => {});
});
